@function color($option){
  @if $option == 'wc.red' { @return rgba(232, 85, 78, 1); }
  @if $option == 'grey.light' { @return rgba(240, 240, 240, 1); };;
}

@mixin triangles($position){
  &::#{$position}{
    position: absolute;
    z-index: 1;
    display: block;
    width: 100%;
    height: 13px;
    background: transparent image-url("triangle/red.svg") repeat-x left top;
    content: "";
    @if $position == "before" {
      top: -13px;
      @include transform(scaleY(-1));
    } @else{
      top: 100%;
    }
  }
}